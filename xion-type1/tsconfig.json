{
  "compilerOptions": {
    // "target": "es5"  // 컴파일 하면 해당 버전에 맞게 변환해서 자바스크립트로 컴파일한다. 예) Array 함수를 컴파일하면 일반 function 으로 변환된다.
    "target": "ESNext",  // 최신 버전으로 컴파일한다.
    "module":"ESNext", // 모듈 시스템을 어떤 것으로 사용할 것인지 설정한다. ESNext는 최신 버전으로 설정한다.
    "outDir": "dist", // 컴파일된 파일을 저장할 경로
    "strict": true, // 엄격한 타입 검사를 할 것인지 설정한다. true로 설정하면 엄격한 타입 검사를 한다.
    "moduleDetection":"force", // 모듈 시스템을 강제로 사용할 것인지 설정한다. force로 설정하면 강제로 사용한다. (컴파일하면 export 자동 생성)
    "skipLibCheck" : true, // 라이브러리 체크를 건너뛸 것인지 설정한다. true로 설정하면 라이브러리 체크를 건너뛴다.
    },
  "include": ["src/**/*.ts"] // 컴파일할 파일 경로
}